{
  "name": "@payflow/webhook-worker",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "@payflow/db": "workspace:*",
    "@payflow/types": "workspace:*",
    "@payflow/utils": "workspace:*",
    "drizzle-orm": "^0.43.0",
    "hono": "^4.8.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^4.0.0"
  }
}
